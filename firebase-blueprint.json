{
  "entities": {
    "SavedSample": {
      "title": "SavedSample",
      "description": "A geochemically analyzed sample or custom rock/mineral profile saved to user collection",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the saved sample" },
        "userId": { "type": "string", "description": "UID of the owner user" },
        "name": { "type": "string", "description": "User assigned sample title or geological specimen label" },
        "sampleType": { "type": "string", "enum": ["rock", "mineral", "custom"], "description": "Type of sample" },
        "oxides": { "type": "object", "description": "Key-value pair map of oxide wt% percentages" },
        "identifiedName": { "type": "string", "description": "Classified rock or mineral name" },
        "confidence": { "type": "number", "description": "Classification confidence percentage" },
        "tasField": { "type": "string", "description": "Total Alkali Silica volcanic classification field" },
        "alkaliAffinity": { "type": "string", "description": "Subalkaline or Alkaline magma affinity" },
        "notes": { "type": "string", "description": "User geological field observations or petrographic notes" },
        "tags": { "type": "array", "description": "List of geological tags" },
        "databaseRefs": { "type": "object", "description": "Cross-references to Mindat, Webmineral, RRUFF, EarthChem" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Creation timestamp" },
        "updatedAt": { "type": "string", "format": "date-time", "description": "Last update timestamp" }
      },
      "required": ["id", "userId", "name", "sampleType", "oxides", "createdAt"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "description": "Profile data for authenticated geologist user",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "User UID" },
        "email": { "type": "string", "description": "User primary email address" },
        "displayName": { "type": "string", "description": "Geologist display name" },
        "photoURL": { "type": "string", "description": "Avatar URL" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Account registration timestamp" }
      },
      "required": ["id", "email", "createdAt"]
    },
    "Feedback": {
      "title": "Feedback",
      "description": "User feedback, bug reports, and geochemical data improvement suggestions",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for feedback document" },
        "userId": { "type": "string", "description": "User UID if signed in, or anonymous" },
        "userEmail": { "type": "string", "description": "Email of submitting user or contact email" },
        "category": { "type": "string", "enum": ["bug", "feature", "data", "ux", "other"], "description": "Feedback category" },
        "rating": { "type": "number", "description": "1-5 star user satisfaction score" },
        "message": { "type": "string", "description": "User feedback message and description" },
        "sampleContext": { "type": "string", "description": "Optional active specimen context" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Creation timestamp" }
      },
      "required": ["id", "category", "rating", "message", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profile document storing basic user details"
    },
    "/users/{userId}/savedSamples/{sampleId}": {
      "schema": "SavedSample",
      "description": "Collection of saved geochemical rock and mineral profiles owned by the user"
    },
    "/feedback/{feedbackId}": {
      "schema": "Feedback",
      "description": "User-submitted feedback, feature ideas, and bug reports"
    }
  }
}
